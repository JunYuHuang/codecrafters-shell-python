# Notes:

- PEDAC: Problem
    - input:
        - `command`: string
            - get from user prompt
            - of any chars
            - of length in range \[0, INF]
    - output: N/A
    - side effects:
        - prints the following to the terminal in stdout (standard output): `{command}: command not found\n`
- PEDAC: Examples
    - TODO
- PEDAC: DS + Algo
    - get user input and save it in string variable `command`
    - print `{command}: command not found\n` to terminal output

